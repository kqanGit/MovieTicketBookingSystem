#include "ShowTime.h"

ShowTime::ShowTime(int showTimeID, const std::string& date, const std::string& startTime, const std::string& endTime)
    : showTimeID(showTimeID), date(date), startTime(startTime), endTime(endTime) {}